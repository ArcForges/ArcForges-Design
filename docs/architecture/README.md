# Architecture

This directory defines system architecture, runtime topology, component boundaries, data ownership, communication protocols, and security models for ArcForges.

All content here is **authoritative**. It is governed by the frozen Phase 1 decisions in [`../decisions/phase-1-foundation-decisions.md`](../decisions/phase-1-foundation-decisions.md) (D-001 … D-023) and by the verification record in [`../assurance/phase-1-official-verification.md`](../assurance/phase-1-official-verification.md) (V-01 … V-09). Architecture realises the requirements in [`../requirements/`](../requirements/README.md); where the two appear to disagree, the requirement states *what* must be true and the architecture states *how*, and any genuine conflict is a defect to be resolved, not a choice to be made at implementation time.

## Reading order

Read [`00-architecture-overview.md`](00-architecture-overview.md) first — it establishes the five controlling constraints, the runtime topology, the layering rules and the single write path that every other document assumes. Then read [`01-solution-and-project-layout.md`](01-solution-and-project-layout.md) and [`02-contracts-and-protocols.md`](02-contracts-and-protocols.md), which fix where code lives and how components speak to each other.

Documents `00`–`17` state architecture: boundaries, ownership and rules. The [`contracts/`](contracts/00-operation-catalogue.md) and [`data-model/`](data-model/00-data-model-overview.md) subdirectories state the **concrete design those rules produce** — the actual operations, signatures, events and schemas. A rule without its concrete counterpart is not yet implementable, which is why they are held to the same authority.

## Documents

| Document | Covers |
|---|---|
| [`00-architecture-overview.md`](00-architecture-overview.md) | Controlling constraints, runtime topology, the three communication responsibilities and four data paths, layering rules, the AOT matrix, the process model, the shared foundation boundary, licence boundaries, the consistency model, the single write path, and the architecture risk register |
| [`01-solution-and-project-layout.md`](01-solution-and-project-layout.md) | The target repository tree, project conventions, contract project split across the licence boundary, licence enforcement, cloud module projects, reference direction, and the architecture and repository-policy test suites |
| [`02-contracts-and-protocols.md`](02-contracts-and-protocols.md) | The two-layer contract model, App/Installation/Instance, contribution kinds, capability descriptors, actions and availability, context freezing, `ResourceRef` rules, artifacts, deep links, events, health dimensions, version compatibility, the invocation pipeline, and contract-authoring obligations |
| [`03-local-ipc-and-process-model.md`](03-local-ipc-and-process-model.md) | Transport per platform, wire format, the endpoint manifest, registration lifecycle, health and backpressure, routing, the connection manager, concurrency, disconnection and retry, local security, large-data rules, the AOT checklist, and the remote bridge |
| [`04-desktop-application-architecture.md`](04-desktop-application-architecture.md) | Desktop host structure, AOT constraints, MVVM and threading, windows and sessions, persistence, journal/snapshot/recovery, long-running work, the cloud client, shell composition, start-up and shutdown sequences, and performance architecture |
| [`05-cloud-architecture.md`](05-cloud-architecture.md) | The JIT modular monolith decision and its evidence, the three runtime roles, host pipeline order, module set, persistence, the public API surface, realtime, reliable events, background work, remote action, tenancy isolation, failure isolation, and scaling posture |
| [`06-data-persistence-and-formats.md`](06-data-persistence-and-formats.md) | Store composition, the canonical commit unit, journal and snapshot, the managed resource store, the large append store, derived stores, device-local state, format architecture and the portable package, migration mechanics, import and export, storage pressure, and Git projection |
| [`07-sync-conflict-and-backup.md`](07-sync-conflict-and-backup.md) | Identity and revision, sync scope, the client outbox and server inbox, the change feed, conflict policies, deletion propagation, blob lifecycle, availability states, protection profiles, data health, backup layers, export and realm migration, and cross-device schema versioning |
| [`08-security-architecture.md`](08-security-architecture.md) | Identity layering, the four enforcement points, authentication, the authorization model, approval architecture, the secret broker and envelope encryption, egress control, untrusted input boundaries, delegation, trust evaluation, audit architecture, web/mobile/network security, and the threat model |
| [`09-ai-and-agent-runtime-architecture.md`](09-ai-and-agent-runtime-architecture.md) | The agent runtime under AOT, the capability registry and selection pipeline, the task engine and its idempotency identities, context assembly, provider routing, metering and budget, approval and steering, placement and remote execution, child tasks, compensation and checkpoints, trace systems, the automation engine, and MCP integration |
| [`10-web-architecture.md`](10-web-architecture.md) | The two build outputs, deterministic static generation, the single Blazor WebAssembly application and its deployment profiles, the surface matrix, browser authentication, content security, offline behaviour, accessibility and performance budgets, analytics, and build-once promotion |
| [`11-mobile-architecture.md`](11-mobile-architecture.md) | MAUI layering and the shared/not-shared split, licence boundary enforcement, the Android Mono AOT runtime posture, source-generated clients, network behaviour, the offline outbox, secure storage, push and deep links, build-verifiable commerce prohibitions, placement constraints, and release gates |
| [`12-native-interop-and-media.md`](12-native-interop-and-media.md) | Where native code is permitted, P/Invoke discipline, the C ABI contract, loading rules, handle lifetime, buffers and the large-data path, threading, the safety obligations that make a worker-free design acceptable, and the ArcSlate media and ArcScope acquisition pipelines |
| [`13-observability-and-operations.md`](13-observability-and-operations.md) | Signal architecture and required dimensions, correlation propagation, redaction by construction, audit architecture, health and service levels, alerting and incidents, the status page, client diagnostics and consent, the operator surface, operational dependency adapters, runbooks, and cost control |
| [`14-build-packaging-and-release.md`](14-build-packaging-and-release.md) | Repository build model and stages, the runtime publish matrix, versioning implementation across the nine axes, packaging per platform, signing and supply chain, the artifact store and update feed, the update client, release gates, CI topology, and environment promotion |
| [`15-extension-platform-architecture.md`](15-extension-platform-architecture.md) | The out-of-process extension host, process supervision and handshake, the dual capability boundary and closed structured value model, contribution kinds, declarative UI contribution, security enforcement, the package runtime and lifecycle, the catalog, versioning and contribution-level compatibility, and the public SDK and CLI |
| [`16-billing-and-commerce-architecture.md`](16-billing-and-commerce-architecture.md) | Commerce module boundaries, billing identity, the purchase pipeline and provider event inbox, the entitlement resolver and its distribution, quota and usage, the credit lot model with reserve-then-settle, the three ledgers, reconciliation, mobile and regional posture, and commercial evidence |
| [`17-agent-harness.md`](17-agent-harness.md) | The concrete model/tool loop: layer separation between provider transport, reusable mechanism, the ArcForges harness and external agents; the durable turn loop; parallel tool batching and resource conflict; request construction, response classification, loop bounds and progress detection; context assembly, staleness invalidation, tool-declaration filtering and history compaction; approval interleaving; durability, cancellation and crash recovery; streaming; provider failure handling; external agents and MCP; multi-agent posture; placement; and transparency and cost |
| [`18-editing-and-rich-content.md`](18-editing-and-rich-content.md) | The document content model and its typed inline structure; the closed edit-transaction set, undo and kind conversion; the agent editing path; selection, grapheme-correct caret, bidi and IME composition; Markdown-friendly input; the rendering stack and block virtualisation; persistence coupling; code, math, images, tables and embeds; the three honest preview levels, the PDF dependency and its open gate, Office and media handling, and untrusted-content boundaries; what ArcForges deliberately does not build; import, export and the clipboard |
| [`19-product-implementation-maps.md`](19-product-implementation-maps.md) | Where each accepted capability lives: the shared five-layer component pattern, a capability-to-component map for ArcChat, ArcNotes, ArcScope and ArcSlate, and the reference verification for each product — including the row-by-row AionUI verification, the AFFiNE licence split, Serial-Studio's Pro-module exclusions and ArcVideo's preserved Olive provenance; plus what no product builds |
| [`20-cross-system-lifecycles.md`](20-cross-system-lifecycles.md) | What happens when a lifecycle spanning two or more systems fails halfway: purchase to entitlement, the subscription and credit lifecycles, sync change propagation, assets and blobs, the five deletions, and device and session revocation — each traced end to end with a failure matrix giving effect certainty, detector, owner, outcome and whether repair is automatic |
| [`21-platform-and-dependency-matrix.md`](21-platform-and-dependency-matrix.md) | The platform tier model and the per-product OS and architecture matrix; the native capability inventory as slots with owners, ABI position and governing gate; what an adoption decision must produce before a dependency ships; managed dependency classes and their constraints; the degradation for every slot when it is unavailable; and the build, signing, SBOM and start-up-verification linkage |
| [`contracts/00-operation-catalogue.md`](contracts/00-operation-catalogue.md) | The shared operation contract every surface obeys: request and response envelopes, the closed error-code table with retryability and effect certainty, the authorization declaration block, idempotency classes, cursor rules, compatibility classes, and surface ownership |
| [`contracts/01-public-api-operations.md`](contracts/01-public-api-operations.md) | Every cloud HTTP operation — identity, workspace and device, entitlement, commerce, sync, resource, task, approval and bridge, chat, agent and knowledge, policy, notification and support — with what is deliberately absent |
| [`contracts/02-local-rpc-operations.md`](contracts/02-local-rpc-operations.md) | The local RPC interface map and signatures: `IHubRegistry`, `IHubRouting`, `ICapabilityProvider`, `IContextProvider`, `IArtifactHandler`, `IResourceAccess`, `IProductLifecycle`, `IDeepLinkTarget`, and the product interfaces `INotesOperations`, `IScopeOperations`, `ISlateOperations` and `IChatOperations` |
| [`contracts/03-realtime-and-bridge.md`](contracts/03-realtime-and-bridge.md) | The realtime event set and its non-authoritative rule, subscriptions and permission re-checking, gap detection and scoped reconciliation, degradation, and the durable tool bridge with local re-authorization, idempotency and effect certainty across the boundary |
| [`data-model/00-data-model-overview.md`](data-model/00-data-model-overview.md) | Schema notation, identifier and revision rules, aggregate roots, the authority map, task ownership across placement, cross-store rules, transaction boundaries and idempotency, the outbox, the five distinct meanings of deletion, the consistency model, indexing, tenancy and schema evolution |
| [`data-model/01-cloud-data-model.md`](data-model/01-cloud-data-model.md) | The cloud schema in full: platform, identity, workspace, device, entitlement, commerce, chat, task, agent, sync and resource modules, with their keys, constraints and cross-module invariants |
| [`data-model/02-desktop-data-model.md`](data-model/02-desktop-data-model.md) | The desktop schema: the shared table groups every product store carries, the per-product schemas for ArcChat, ArcNotes, ArcScope and ArcSlate, and the portable package |
| [`data-model/03-derived-stores.md`](data-model/03-derived-stores.md) | The derived-store contract, the search index and its citation anchors, retrieval chunks and embedding staleness, cache isolation, the per-product derived store table, and eviction |

## Conventions

- Every architectural rule carries a stable identifier (`AB-nn`) scoped to its document, so requirements, assurance and work packages can cite it precisely.
- A rule states an obligation that a reviewer or a test can check. Where a rule is machine-checkable, the corresponding architecture or repository-policy test is named in [`01-solution-and-project-layout.md`](01-solution-and-project-layout.md) or in the document's own testing section.
- Citations to Phase 1 decisions use **D-nnn**, verification findings use **V-nn**, and deferred gates use **F-nnn**.
- Citations to preserved inputs use `I1`–`I4` with a section reference. Those files are historical evidence and are never modified.
- Superseded product names (`ArcCanvas`, `ArcMusic`, `ArcImage`, `ArcVideo`) and the superseded payment provider never appear as current, per **D-002** and **D-005**.
