# Architecture

This directory owns accepted system structure, runtime topology, component boundaries, data ownership, communication, security, deployment, and quality-attribute design.

Architecture specifications are created from accepted requirements and reviewed design inputs. They describe current intended structure rather than migration history.
