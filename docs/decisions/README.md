# Architecture Decisions

This directory contains numbered records for significant, difficult-to-reverse choices that affect system structure or key quality attributes.

Use `NNNN-short-descriptive-title.md`. Accepted records are immutable in meaning. A changed decision requires a new record that links to and supersedes the earlier record.
